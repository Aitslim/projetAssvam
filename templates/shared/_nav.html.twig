<!-- Menu de navigation -->
<nav class="navbar navbar-expand-md fixed-top bg-dark navbar-dark" style="background-color: #212121;">
	<a class="navbar-brand" class="nav-link" href="{{ path('home') }}">
		<img src="{{ asset('assets/images/Village.jpg') }}" alt="logo" style="width:30px; height:30px">
	</a>
	<ul class="navbar-nav">
		<li class="nav-item">
			<span class="navbar-text">NomVillage</span>
		</li>
	</ul>

	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsibleNavbar" aria-controls="collapsibleNavbar" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="collapsibleNavbar">

		<ul class="navbar-nav ml-auto">
			<li class="nav-item active">
				<a class="nav-link" href="{{ path('home') }}">
					<i class="fas fa-home mr-1"></i>Accueil</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('home') }}">
					<i class="far fa-question-circle mr-1"></i>A propos</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('home') }}">
					<i class="fas fa-envelope mr-1"></i>Contact</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('home') }}">
					<i class="fas fa-envelope mr-1"></i>Autre</a>
			</li>

			{% if is_granted("ROLE_ADMIN") %}
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" id="dropdown01" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Admin</a>
					<div class="dropdown-menu" aria-labelledby="dropdown01">
						<a class="dropdown-item" data-toggle="collapse" data-target=".navbar-collapse.show">Article</a>
						<a class="dropdown-item" data-toggle="collapse" data-target=".navbar-collapse.show">Agenda</a>
						<a class="dropdown-item" data-toggle="collapse" data-target=".navbar-collapse.show">Utilisateurs</a>
						<a class="dropdown-item" data-toggle="collapse" data-target=".navbar-collapse.show">Diaporama</a>
						<a class="dropdown-item" data-toggle="collapse" data-target=".navbar-collapse.show">Menu x</a>
						<a class="dropdown-item" data-toggle="collapse" data-target=".navbar-collapse.show">Menu y</a>
					</div>
				</li>
			{% endif %}

			{% if not app.user %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_register') }}">
						<i class="fas fa-user mr-1"></i>Inscription</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_login') }}">
						<i class="fas fa-sign-in-alt mr-2"></i>Connexion</a>
				</li>
			{% else %}
				<li class="nav-item">
					<span class="navbar-text">{{ app.user.pseudo }}</span>
				</li>
				<li class="nav-item">
					<a href="{{ path('app_logout') }}" class="nav-link">
						<i class="fas fa-sign-out-alt mr-2"></i>Déconnexion</a>
				</li>
			{% endif %}
		</ul>

	</div>
</nav>
